let arr = [1, 2, 3, 4, 5];
arr.fill(1, 1, 5);

/*
  We can find out whether it's destructive by checking value of original array after calling the method
  We can also check the documentation on mdn 
*/
